# 视觉码垛控制系统开发：
## 准备工作
- 视觉通信软件（海康相机 + vision master）
- 机器人(SCARA四轴机器人)
- 配置对应IP,电脑IP需与通信软件和机器人在同一IP网段下（机器人IP:168.168.40.20,通信IP:168.168.40.111,端口：4004），本机IP需设置为同一网段
- 对应机器人封装好的api(xapi)
- 项目最终实现效果，借助pyqt5绘制简单界面，然后对“工件”进行简单搬运（仅演示搬运轨迹）
### 注意
该项目中test文件为前期开发的快速迭代版本，最终呈现版本为robot_pallet文件夹下的内容，主入口为main.py。

## 使用过程中的一些注意点：
- 面对视觉数据通信，借助通信软件对工件进行模板标定，实现像素坐标与真实物理坐标的转换。要求误差尽量小（同一演示作业下可使用视觉标定的保存与导入功能，其他情况禁止使用，否则将导致标定不准确，易发射撞机等安全情况）
- 码垛工艺逻辑需根据实际情况进行填写数值，注意Z(高度)取值范围，否则容易撞到平面平台，或者出现J3轴正软限位报错（码垛需设计合理）
- 注意旋转角度C值，本演示系统基于J2轴相机进行演示，如果发现演示过程中C值出现转换不对的情况，本系统提供了一种解决方案，读取安全点位的C值覆盖转换C值（即手动C值覆盖转换的C值）以防撞机。
- 界面上的急停按钮并不能实现物理意义上的急停，（仅实现清空队列命令逻辑）所以在演示过程中，速度尽量要小，且随着按急停按钮。